% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Scoring.R
\name{scoring_matrix}
\alias{scoring_matrix}
\title{Build a Substitution (Scoring) Matrix}
\usage{
scoring_matrix(type = c("nucleotide", "BLOSUM62"), match = 2, mismatch = -1)
}
\arguments{
\item{type}{One of \code{"nucleotide"} (default) or \code{"BLOSUM62"}.}

\item{match}{Score for identical residues (nucleotide scheme only).}

\item{mismatch}{Score for differing residues (nucleotide scheme only).}
}
\value{
A numeric matrix with row/column names equal to the residue
alphabet, suitable for passing to \code{\link{align}}.
}
\description{
Constructs a named symmetric substitution matrix used to score aligned
residues. Two ready-made schemes are provided: a simple nucleotide matrix
(match / mismatch) for DNA/RNA, and the BLOSUM62 matrix for proteins.
You can also supply your own \code{match} / \code{mismatch} values.
}
\examples{
m <- scoring_matrix("nucleotide", match = 2, mismatch = -1)
m["A", "A"]  # 2
m["A", "G"]  # -1
p <- scoring_matrix("BLOSUM62")
p["W", "W"]  # 11

}
