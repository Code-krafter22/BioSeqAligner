% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Batch_Alignment.R
\name{batch_align}
\alias{batch_align}
\title{Align One Query Against Many References (mini-BLAST)}
\usage{
batch_align(
  query,
  refs,
  method = c("local", "global"),
  submat = scoring_matrix("nucleotide"),
  gap_open = 10,
  gap_extend = 1,
  top = NULL
)
}
\arguments{
\item{query}{A character string, the query sequence.}

\item{refs}{A named character vector of reference sequences (e.g. from
\code{\link{read_fasta}}).}

\item{method}{\code{"local"} (default, good for finding subregions) or
\code{"global"}.}

\item{submat}{Substitution matrix (see \code{\link{scoring_matrix}}).}

\item{gap_open, gap_extend}{Affine gap penalties.}

\item{top}{If not \code{NULL}, keep only the top \code{n} hits.}
}
\value{
A data frame ranked by score, with reference name, score, percent
identity, query coverage (percent of the query length actually aligned —
important for local alignment, where a short coincidental match can
otherwise show a misleadingly high identity), alignment length, and the
reference match coordinates.
}
\description{
Aligns a single query sequence against a set of reference sequences and
returns a ranked results table (best score first), giving a lightweight
BLAST-like search over a small, in-memory database.
}
\examples{
refs <- c(refA = "TTACGTGGATT", refB = "GGGGCCCC", refC = "AAACGTGGAAA")
batch_align("ACGTGGA", refs, method = "local")

}
